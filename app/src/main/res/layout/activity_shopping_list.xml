<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- TextView to Display Selected Items -->
    <TextView
        android:id="@+id/itemsTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Selected Items:"
        android:textSize="22sp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:paddingBottom="16dp"
        android:textColor="@android:color/black" /> <!-- Make sure the text color is visible -->

    <!-- Button to Mark All as Purchased -->
    <Button
        android:id="@+id/purchaseButton"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:text="Mark as Purchased"
        android:textSize="14sp"
        android:textColor="@android:color/white"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="100dp"
        android:layout_alignParentBottom="true"
        android:padding="12dp"
        android:backgroundTint="@color/purple_700"
        android:background="@drawable/rounded_button" /> <!-- Apply the rounded corners -->

</RelativeLayout>
